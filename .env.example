# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your actual values
#   source .env  # or use with scripts
# =============================================================================

# -----------------------------------------------------------------------------
# GCP/GKE Configuration
# -----------------------------------------------------------------------------
# Google Cloud Platform project and cluster settings

# GKE cluster name
CLUSTER_NAME="your-gke-cluster-name"

# GKE cluster zone (e.g., asia-southeast1-b, us-central1-a)
CLUSTER_REGION="asia-southeast1-b"

# GCP project ID
PROJECT_ID="your-gcp-project-id"

# Docker image name for GCR
IMAGE_NAME="rust-datadog-otel"

# GCP region for services (e.g., asia-southeast1, us-central1)
REGION="asia-southeast1"

# -----------------------------------------------------------------------------
# Kubernetes Configuration
# -----------------------------------------------------------------------------
# Kubernetes deployment settings

# Kubernetes namespace for the application
NAMESPACE="rust-test"

# Kubernetes service name
SERVICE_NAME="rust-datadog-otel"

# Local port for kubectl port-forward
LOCAL_PORT="8080"

# Kubernetes service port (internal)
SERVICE_PORT="80"

# -----------------------------------------------------------------------------
# Datadog Configuration
# -----------------------------------------------------------------------------
# Datadog Unified Service Tagging and APM settings
# Reference: https://docs.datadoghq.com/getting_started/tagging/unified_service_tagging/

# Service name (appears in Datadog APM)
DD_SERVICE="rust-datadog-otel"

# Application version (for tracking deployments)
DD_VERSION="0.1.0"

# Environment name (local, development, staging, production)
DD_ENV="local"

# Datadog Agent host
# - For local development: "localhost"
# - For Kubernetes: Set via fieldPath status.hostIP in deployment.yaml
DD_AGENT_HOST="localhost"

# Datadog Agent trace port (default: 8126)
DD_AGENT_PORT="8126"

# Enable Datadog tracing
DD_TRACE_ENABLED="true"

# Enable automatic trace ID injection into logs
DD_LOGS_INJECTION="true"

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Application-specific settings

# Rust log level
# Format: "level,crate_name=level"
# Levels: error, warn, info, debug, trace
RUST_LOG="info,rust_datadog_otel=debug"

# -----------------------------------------------------------------------------
# API Keys (SECRETS - DO NOT COMMIT!)
# -----------------------------------------------------------------------------
# ⚠️  NEVER commit actual values to version control!
# ⚠️  These should be stored securely (1Password, AWS Secrets Manager, etc.)

# Datadog API Key
# Get from: https://app.datadoghq.com/organization-settings/api-keys
DD_API_KEY="your-datadog-api-key-here"

# Datadog Application Key
# Get from: https://app.datadoghq.com/organization-settings/application-keys
DD_APP_KEY="your-datadog-app-key-here"

# Google Gemini API Key (if using Gemini features)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key-here"

# OpenAI API Key (if using OpenAI features)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key-here"

# Anthropic API Key (if using Claude features)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY="your-anthropic-api-key-here"

# =============================================================================
# Notes
# =============================================================================
#
# 1. Security:
#    - .env is in .gitignore - it will NOT be committed
#    - Rotate API keys regularly
#    - Use different keys for different environments
#    - Never share API keys in chat/email/slack
#
# 2. Kubernetes Secrets:
#    - For production, use Kubernetes secrets:
#      ./scripts/create-secrets.sh
#    - API keys are stored in K8s secrets, not in .env
#
# 3. Local Development:
#    - Make sure Datadog Agent is running locally
#    - Or use port-forward to K8s agent:
#      kubectl port-forward -n datadog svc/datadog 8126:8126
#
# 4. Environment-Specific Values:
#    - local: For local development
#    - development: For dev cluster
#    - staging: For staging environment
#    - production: For production (use strict settings)
#
# =============================================================================
